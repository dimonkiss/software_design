# MultiThread Flow Translator

![Build Status](https://img.shields.io/github/actions/workflow/status/username/repo/tests.yml?branch=main)
![Python](https://img.shields.io/badge/python-3.10%2B-blue)
![License](https://img.shields.io/badge/license-MIT-green)

**MultiThread Flow Translator** — це десктопний додаток для візуального проєктування, симуляції та генерації коду багатопотокових алгоритмів. Програма дозволяє створювати блок-схеми для кількох потоків, налаштовувати спільні змінні та транслювати логіку в робочий Python-код або виконувати її у вбудованому емуляторі.

## 🚀 Основні можливості

* **Візуальний редактор:** Створення блок-схем за допомогою Drag & Drop (Start, Assign, Decision, Input, Print, End).
* **Багатопотоковість:** Підтримка до **100 потоків** одночасно (кожен потік у своїй вкладці).
* **Робота зі змінними:**
    * Спільна пам'ять для всіх потоків.
    * Підтримка до 100 змінних (`var0`...`var99`).
    * Операції присвоєння: `V = Const` (число) або `V1 = V2` (інша змінна).
    * Підтримка 32-бітних беззнакових цілих чисел (0...2³¹-1).
* **Зв'язки:** Зручне створення стрілок між блоками (ПКМ + перетягування).
* **Вбудований інтерпретатор:** Запуск та виконання алгоритму прямо в програмі з виводом у віртуальні термінали.
* **Генерація коду:** Експорт логіки у чистий Python-файл з використанням бібліотеки `threading`.
* **Збереження/Завантаження:** Повна підтримка серіалізації проєкту у формат JSON.
* **Валідація:** Автоматична перевірка на помилки (відсутність Start/End, "висячі" блоки, переповнення значень тощо).

## 🛠️ Встановлення та Запуск

### Передумови
* Python 3.10 або новіше.
* Git (для клонування репозиторію).

### Кроки

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [https://github.com/your-username/MultiThreadFlow.git](https://github.com/your-username/MultiThreadFlow.git)
    cd MultiThreadFlow
    ```

2.  **Створіть віртуальне середовище (рекомендовано):**
    ```bash
    python -m venv .venv
    # Windows:
    .venv\Scripts\activate
    # Linux/Mac:
    source .venv/bin/activate
    ```

3.  **Встановіть залежності:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустіть програму:**
    ```bash
    python main.py
    ```

## 📖 Інструкція користувача

1.  **Створення потоків:**
    * Натисніть кнопку `Додати потік` на панелі інструментів або `Ctrl+T`.
    * Кожен потік відкривається в новій вкладці.
2.  **Додавання блоків:**
    * Натисніть на відповідну кнопку на панелі (`+ START`, `+ ASSIGN` тощо).
    * Блок з'явиться на сцені активної вкладки.
3.  **З'єднання блоків:**
    * Затисніть **Праву Кнопку Миші** на блоці-джерелі.
    * Протягніть лінію до блоку-цілі та відпустіть кнопку.
4.  **Редагування властивостей:**
    * Клікніть **Лівою Кнопкою Миші** на блок.
    * У панелі праворуч змініть параметри (змінні, значення, умови).
5.  **Видалення:**
    * Виділіть блок або стрілку і натисніть `Delete`.
6.  **Запуск:**
    * Натисніть кнопку ▶️ ("Транслювати") або `F5`.
    * Якщо помилок немає, відкриється вікно симуляції з терміналами для кожного потоку.

## 📂 Структура проєкту

```text
MultiThreadFlow/
│
├── src/
│   ├── core/               # Логічне ядро
│   │   ├── models.py       # Структури даних (Project, Thread, Block)
│   │   ├── serializer.py   # Збереження/Завантаження JSON
│   │   ├── validator.py    # Перевірка помилок
│   │   └── engine/         # Виконання коду
│   │       ├── generator.py    # Генерація .py файлів
│   │       └── interpreter.py  # Емулятор виконання
│   │
│   └── gui/                # Графічний інтерфейс (PySide6)
│       ├── main_window.py  # Головне вікно
│       ├── properties.py   # Панель властивостей
│       ├── execution_window.py # Вікно терміналів
│       ├── canvas/         # Робота з графікою (сцена, блоки, стрілки)
│       └── widgets/        # Допоміжні віджети (вкладки, термінал)
│
├── tests/                  # Автотести (pytest)
├── main.py                 # Точка входу
└── requirements.txt        # Список бібліотек
```

## 🧪 Тестування

**Проєкт покритий Unit-тестами (логіка) та GUI-тестами.**

**Щоб запустити тести:**

```bash
  python -m pytest tests
```

## ⚙️ CI/CD

**Проєкт налаштований на використання GitHub Actions:**

* Tests: При кожному push код автоматично тестується на Windows (Python 3.10).